# Meta MCP Server - Environment Configuration
#
# INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Fill in your API keys below
# 3. NEVER commit .env to version control
#
# This file contains sensitive credentials for external API providers.

# ==========================================================================
# ANTHROPIC API (Required for Claude models)
# ==========================================================================
# Get your API key from: https://console.anthropic.com/settings/keys
# Used by LiteLLM to route requests to Claude models
ANTHROPIC_API_KEY=sk-ant-api03-...

# ==========================================================================
# OPENAI API (Required for GPT models)
# ==========================================================================
# Get your API key from: https://platform.openai.com/api-keys
# Used by LiteLLM to route requests to GPT models
OPENAI_API_KEY=sk-...

# ==========================================================================
# LITELLM (Optional)
# ==========================================================================
# Master key for LiteLLM proxy authentication
# If not set, LiteLLM will run without authentication
# Generate a secure key: openssl rand -hex 32
LITELLM_MASTER_KEY=

# ==========================================================================
# OPEN WEBUI (Optional)
# ==========================================================================
# Secret key for Open WebUI session encryption
# If not set, Open WebUI will generate one automatically
# Generate a secure key: openssl rand -hex 32
WEBUI_SECRET_KEY=

# ==========================================================================
# REDIS (Optional - uses default if not set)
# ==========================================================================
# Redis connection URL for meta-supervisor governance state
# Default: redis://localhost:6379
# REDIS_URL=redis://localhost:6379

# ==========================================================================
# META-SUPERVISOR (Optional - uses defaults if not set)
# ==========================================================================
# Host and port for meta-supervisor MCP server
# Default: 0.0.0.0:8001
# HOST=0.0.0.0
# PORT=8001

# Workspace root directory for file operations
# Default: ./workspace
# WORKSPACE_ROOT=./workspace

# Audit log path
# Default: ./audit.jsonl
# AUDIT_LOG_PATH=./audit.jsonl

# ==========================================================================
# AI PROVIDER CONFIGURATION
# ==========================================================================
# Embedding provider type:
# - "openai-compatible": Any OpenAI-compatible endpoint (OpenAI, Ollama, vLLM, LiteLLM, etc.)
# - "gemini": Google AI Gemini embeddings
EMBEDDING_PROVIDER=openai-compatible

# Base URL for embedding endpoint
# Common values:
# - OpenAI: https://api.openai.com/v1
# - Ollama local: http://localhost:11434/v1
# - LiteLLM proxy: http://localhost:4000/v1
# - Azure OpenAI: https://<resource>.openai.azure.com/openai/deployments/<deployment>
EMBEDDING_BASE_URL=https://api.openai.com/v1

# API key/token for embedding endpoint
# OpenAI: sk-...
# Ollama local: usually blank
# LiteLLM proxy: can be your virtual key
# Azure OpenAI: your Azure API key
EMBEDDING_API_KEY=

# Embedding model name
# Common values:
# - OpenAI: text-embedding-3-small
# - Ollama: nomic-embed-text
# - LiteLLM proxy: provider/model alias configured in proxy
# - Azure OpenAI: your deployment name
EMBEDDING_MODEL=text-embedding-3-small

# Embedding vector dimension (must match model output)
# Examples:
# - text-embedding-3-small: 1536
# - text-embedding-3-large: 3072
# - Gemini text-embedding-004: 768
EMBEDDING_DIMENSION=1536

# LLM inference endpoint base URL
# Common values:
# - OpenAI: https://api.openai.com/v1
# - Ollama local: http://localhost:11434/v1
# - LiteLLM proxy: http://localhost:4000/v1
# - Azure OpenAI: https://<resource>.openai.azure.com/openai/deployments/<deployment>
LLM_BASE_URL=

# API key/token for the LLM endpoint
LLM_API_KEY=

# LLM model or deployment name
# Examples: gpt-4o-mini, llama3.1, claude-3-5-sonnet, your-azure-deployment
LLM_MODEL=gpt-4o-mini

# Qdrant vector database endpoint and collection defaults
# Local default endpoint: http://localhost:6333
QDRANT_URL=http://localhost:6333
# Qdrant API key (blank for local/dev without auth)
QDRANT_API_KEY=
# Default collection used for chunk storage
QDRANT_COLLECTION=chunks_v1

# Tokenizer encoding used for chunking/token estimates
# Default for OpenAI-compatible models: cl100k_base
CHUNKER_ENCODING=cl100k_base

# ==========================================================================
# NOTES
# ==========================================================================
# - API keys are loaded into Docker containers via docker-compose.yaml
# - Meta-supervisor runs on HOST (not in Docker) and reads from .env if using python-dotenv
# - For security, ensure .env is in .gitignore
# - Rotate API keys regularly
# - Use environment-specific .env files for dev/staging/prod
